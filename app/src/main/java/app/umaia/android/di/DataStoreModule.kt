package app.umaia.android.di

// All DataStore/Preferences classes use @Singleton @Inject constructor with @ApplicationContext.
// Hilt provides them automatically — no manual bindings needed.
// This module is kept as a placeholder in case future manual DataStore config is needed.
